{
  "projectSettings": {
    "framework": "nextjs",
    "rootDirectory": "./",
    "buildCommand": "npm run build --workspace=frontend",
    "installCommand": "npm install",
    "outputDirectory": ".next"
  }
}
